import React from 'react';

import { Row } from 'reactstrap';

import './styles.scss';

const Failed = ({ history }) => {
  return (
    <Row className="failWrapper">
      a
    </Row>
  )
}

export default Failed;